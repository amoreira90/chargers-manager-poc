global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: chargers-manager
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - chargers-app:8080
